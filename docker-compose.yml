version: '3.9'

services:
  hvs_pg:
    container_name: hvs_pg
    image: docker.io/postgres:15-alpine
    networks:
      - backend-network
    environment:
      - POSTGRES_DB=daedalus
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - hvs_pg_data:/var/lib/postgresql/data
  hvs_app:
    container_name: hvs_app
    image: gestanestle/server:1.1
    ports:
      - "8081:8081"
    networks:
      - backend-network
    environment:
      - PG_HOST=hvs_pg

networks:
  backend-network:
    name: backend-network

volumes:
  hvs_pg_data:
    name: hvs_pg_data